# Claude Code Instructions

Read and follow @AGENTS.md as the canonical repository instructions.

When working under a directory with a nested `AGENTS.md`, follow those additional scoped rules as well.

Repository skills live under `.agents/skills/`. When `AGENTS.md` requires a named skill, read that skill's complete `SKILL.md` and follow its referenced resources before acting.
